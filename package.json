{
  "name": "leviacore-site",
  "type": "module",
  "version": "0.1.0",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "verify": "node scripts/verify-pages.mjs",
    "build:verify": "astro build && node scripts/verify-pages.mjs",
    "assets:localize": "node scripts/localize-assets.mjs"
  },
  "dependencies": {
    "@fontsource-variable/syne": "^5.3.0",
    "@fontsource/roboto-mono": "^5.3.0",
    "astro": "^7.2.0",
    "js-yaml": "^5.2.3"
  },
  "private": true,
  "description": "www.leviacore.com — static site (Astro). Migrated off Webflow.",
  "devDependencies": {
    "@types/js-yaml": "^4.0.9"
  }
}
